Place any UI icon assets here if you want to swap out the inline SVG icons used in index.html/style.css.
Currently all icons (services, nav, socials) are inline SVG in index.html, so no external icon files are required to run the site.
